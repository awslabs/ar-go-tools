taint-tracking-problems:
 - sources:
    - method: "source"
      package: "((main)|(command-line-arguments)|(agent-example))"
   sinks:
    - package: "agent-example/log"
      interface: "Logger"

options:
    log-level: 4
    report-paths: true
