syntactic-problems:
  struct-inits:
    - struct:
        type: ".*target$"
      fields-set:
        - field: "x"
          value:
            package: "(field-val)|(main)|(command-line-arguments)"
            const: "^One$"
