options:
  log-level: 3
  silence-warn: true
  reports-dir: .
dataflow-problems:
  user-specs:
    - user-specs.yaml
  field-sensitive-funcs: [ ".*" ]
  taint-tracking:
    - tag: "source-to-sink"
      sources:
        - method: "^Source$"
      sinks:
        - method: "^Sink$"
