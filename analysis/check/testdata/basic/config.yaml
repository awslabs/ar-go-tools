options:
  log-level: 3
  silence-warn: true
  reports-dir: .
dataflow-problems:
  taint-tracking:
    - tag: "source-to-sink"
      sources:
        - method: "^Source$"
      sinks:
        - method: "^Sink$"
