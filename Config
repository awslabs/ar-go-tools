package.ARG-GoAnalyzer = {
    interfaces = (1.0);

    build-system = bgo-wrap-make;

    build-tools = {
        1.0 = {
            GoLang = 1.x;
            BrazilMakeGo = 1.1;
        };
    };

    deploy = {
        map = (default, "-gopath/**");
    };

    dependencies = {
        1.0 = {
            Go3p-Golang-X-Term = 0.x;
            Go3p-Golang-X-Tools = 1.x;
        };
    };

    runtime-dependencies = {
        1.0 = {
        };
    };

};
